Search Somnium's memory for information relevant to $ARGUMENTS.

Use the `mcp__somnium__memory_search` tool with:
- query: "$ARGUMENTS"
- scope: "all"
- top_k: 5

If no results are found, try broadening the query or searching with scope "global" then "project" separately.

Present each hit with its score, scope, title, and a brief excerpt of the content.
