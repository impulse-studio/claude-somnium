Show the full Somnium health snapshot.

Run via Bash:

```bash
somnium status
```

Summarize the output concisely:
- Memory indexes: how many files and chunks in global vs project
- Code index: built or not, how many files
- Hooks: all three registered?
- MCP server: connected?
- Voyage key: set?
- Dream mode: enabled?
