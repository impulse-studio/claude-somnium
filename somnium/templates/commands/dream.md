Force the Somnium dream consolidation agent to run on the most recent session transcript for this project.

Run the following command via Bash and report what was extracted:

```bash
somnium dream --force
```

Report:
- The gate decision and reason
- How many items were persisted (and their categories)
- The digest file path
- If should_persist was false, explain why nothing was saved
